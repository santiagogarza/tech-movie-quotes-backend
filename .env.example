# Optional. HTTP port for the Hono server. Defaults to 8787.
PORT=8787

# Optional. CORS origin allow-list (comma-separated). Defaults to "*"
# so the local Vite dev server (http://localhost:5173) can reach it.
CORS_ORIGIN=*
